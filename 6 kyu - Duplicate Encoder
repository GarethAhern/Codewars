def duplicate_encode(word):
    
    word = word.lower()
    
    output = ""
    
    for letter in word:
        if word.count(letter) == 1:
            output = output + "("
        else:
            output = output +")"

    return output
